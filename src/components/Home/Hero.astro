<section class="hero min-h-screen flex items-center relative overflow-hidden">
  <div class="absolute inset-0 bg-gradient-to-br from-[#0A0A0A] via-[#0F0F0F] to-[#0A0A0A] z-0">
  </div>

  <div class="absolute inset-0 grid-lines opacity-10 z-0"></div>

  <div class="container relative z-10">
    <div class="max-w-3xl mx-auto mt-16 px-4 sm:px-6">
      <div class="space-y-6 sm:space-y-8">
        <div class="overflow-hidden">
          <h1
            class="hero-heading text-4xl sm:text-6xl md:text-7xl lg:text-8xl font-extralight leading-none"
          >
            <span class="block stagger-1">Digital</span>
            <span
              class="block font-bold text-transparent bg-clip-text bg-gradient-to-r from-[#7B68EE] to-[#9370DB] stagger-2"
              >Self Sovereignty</span
            >
            <span class="block stagger-3">For Everyone</span>
          </h1>
        </div>

        <p class="text-lg sm:text-xl md:text-2xl font-light text-gray-300 max-w-xl stagger-4">
          Building freedom technology through cryptography, FOSS & open protocols.
        </p>

        <div class="flex flex-wrap gap-4 stagger-5">
          <a href="/blog/our-vision" class="btn btn-primary w-full sm:w-auto">
            Our Vision
            <span data-lucide="arrow-right" class="ml-2 w-4 h-4"></span>
          </a>
          <a href="#projects" class="btn btn-secondary w-full sm:w-auto">
            Our Projects
          </a>
        </div>
      </div>
    </div>
  </div>

  <div
    class="absolute bottom-0 right-0 w-96 h-96 bg-gradient-to-br from-[#7B68EE] to-[#9370DB] rounded-full filter blur-3xl opacity-10 transform translate-x-1/3 translate-y-1/3 animate-pulse"
  >
  </div>
  <div
    class="absolute top-0 left-0 w-64 h-64 bg-gradient-to-br from-[#7B68EE] to-[#9370DB] rounded-full filter blur-3xl opacity-5 transform -translate-x-1/3 -translate-y-1/3 animate-pulse"
  >
  </div>
</section>

<style>
  .hero {
    perspective: 1000px;
  }

  .grid-lines {
    background-image: linear-gradient(rgba(123, 104, 238, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(123, 104, 238, 0.1) 1px, transparent 1px);
    background-size: 40px 40px;
    background-position: center center;
    transform: rotateX(45deg) scale(1.5);
    transform-origin: center bottom;
  }

  .hero-heading {
    letter-spacing: -0.05em;
  }

  .stagger-1 {
    animation: fadeSlideUp 0.6s 0.1s forwards cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
  }
  .stagger-2 {
    animation: fadeSlideUp 0.6s 0.3s forwards cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
  }
  .stagger-3 {
    animation: fadeSlideUp 0.6s 0.5s forwards cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
  }
  .stagger-4 {
    animation: fadeSlideUp 0.6s 0.7s forwards cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
  }
  .stagger-5 {
    animation: fadeSlideUp 0.6s 0.9s forwards cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
  }

  @keyframes fadeSlideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 0.1;
    }
    50% {
      opacity: 0.15;
    }
  }

  .animate-pulse {
    animation: pulse 8s infinite ease-in-out;
  }

  @media (max-width: 640px) {
    .hero-heading {
      letter-spacing: -0.03em;
    }

    .grid-lines {
      transform: rotateX(45deg) scale(2);
    }
  }
</style>

<script>
  import { createIcons, ArrowRight } from 'lucide';
  
  document.addEventListener("DOMContentLoaded", () => {
    createIcons({
      icons: {
        ArrowRight
      }
    });
  });
</script>
